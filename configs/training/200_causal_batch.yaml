name: causal_batch
causal: True
causal_type: causal_batch
causal_l: 100
train_cfg: {
  lr: 0.0001,
  lr_decay: 0.9,
  lr_step: 10,
  epochs: 30,
  weight_decay: 0.000001,
  weight_init: 'xavier_normal',
  weighted_loss: True,
}
loader: {
  train_batch_size: 200,
  test_batch_size: 100,
}